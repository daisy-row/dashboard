#!/bin/bash
set -e

if [[ `git status --porcelain` ]]; then
  git config --global user.email "ry@linux.com"
  git config --global user.name "Ry Jones"
  git add .
  git commit -s -m "`date -u` update"
  git push origin gh-pages
else
  echo No changes
fi



